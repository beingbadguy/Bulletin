import Header from "@editorjs/header";
import Checklist from "@editorjs/checklist";
import EditorjsList from "@editorjs/list";
import CodeTool from "@editorjs/code";
import Marker from "@editorjs/marker";
import InlineCode from "@editorjs/inline-code";
import Delimiter from "@editorjs/delimiter";
import LinkTool from "@editorjs/link";

export const EDITOR_JS_TOOLS = {
  header: Header,
  checklist: Checklist, // Use the correct capitalization
  list: EditorjsList, // Use the correct capitalization
  code: CodeTool, // Use the correct capitalization
  marker: Marker, // Use the correct capitalization
  inlineCode: InlineCode, // Use the correct capitalization
  delimiter: Delimiter, // Use the correct capitalization
  link: LinkTool, // Use the correct capitalization
};
